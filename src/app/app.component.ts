import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  // Si el html es muy corto, se puede insertar aqui mismo sin tener que llamar a un archivo entero
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent { /* AppComponent limpio :) */ }
